import { connect } from 'react-redux'
import Drukarki3D from './Drukarki3D'

const mapActionCreators = {
}
const mapStateToProps = ({
}) => ({
})

export default connect(mapStateToProps, mapActionCreators)(Drukarki3D)
