import { connect } from 'react-redux'
import WolfSilver from './WolfSilver'

const mapActionCreators = {
}
const mapStateToProps = ({
}) => ({
})

export default connect(mapStateToProps, mapActionCreators)(WolfSilver)
