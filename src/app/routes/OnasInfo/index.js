import { connect } from 'react-redux'
import OnasInfo from './OnasInfo'

const mapActionCreators = {
}
const mapStateToProps = ({
}) => ({
})

export default connect(mapStateToProps, mapActionCreators)(OnasInfo)
