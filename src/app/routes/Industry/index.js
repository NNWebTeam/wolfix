import { connect } from 'react-redux'
import Industry from './Industry'

const mapActionCreators = {
}
const mapStateToProps = ({
}) => ({
})

export default connect(mapStateToProps, mapActionCreators)(Industry)
