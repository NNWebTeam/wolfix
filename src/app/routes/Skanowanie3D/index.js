import { connect } from 'react-redux'
import Skanowanie3D from './Skanowanie3D'

const mapActionCreators = {
}
const mapStateToProps = ({
}) => ({
})

export default connect(mapStateToProps, mapActionCreators)(Skanowanie3D)
