
import { connect } from 'react-redux'
import WolfTeam from './WolfTeam'

const mapActionCreators = {
}
const mapStateToProps = ({
}) => ({
})

export default connect(mapStateToProps, mapActionCreators)(WolfTeam)
