import { connect } from 'react-redux'
import AddNewsModal from './AddNewsModal'

const mapActionCreators = ({
})
const mapStateToProps = ({}) => ({})

export default connect(mapStateToProps, mapActionCreators)(AddNewsModal)